import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OnepartyAllitemReportPage } from './oneparty-allitem-report.page';

describe('OnepartyAllitemReportPage', () => {
  let component: OnepartyAllitemReportPage;
  let fixture: ComponentFixture<OnepartyAllitemReportPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OnepartyAllitemReportPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
